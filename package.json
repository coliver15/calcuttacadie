{
  "name": "calcutta-caddie",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix admin-portal",
    "dev": "npm run dev --prefix admin-portal",
    "start": "npm run start --prefix admin-portal"
  },
  "dependencies": {
    "next": "14.2.0"
  }
}
